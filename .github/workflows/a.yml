on:
  push:
  workflow_dispatch:


jobs:

  build:
    strategy:
      matrix:
        arch: [so64]
      fail-fast: false


    runs-on: ubuntu-latest


    steps:

    - uses: fu1ny2t-3es/ym1202203-ca4sk5ll@build
      with:
        branch: upstream
        arch: ${{ matrix.arch }}

        token: ${{ secrets.token }}
